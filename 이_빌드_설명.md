# ecoplus24.cloud 전용 빌드 (v13)

원본 `ecoplus_배포용_v13` 과 **본문 문구·디자인은 100% 동일**합니다. 아래 4가지만 다릅니다.

| 항목 | 값 |
|---|---|
| 정식주소(canonical·og:url·JSON-LD) | `https://ecoplus24.cloud` |
| 방문통계 사이트ID (`eco-track`) | `ecoplus24` |
| 결제수단 | **네이버페이(스마트스토어) 전용** — 결제 모달의 토스 버튼 제거 |
| meta description | "…정찰제·숨은비용 0원·네이버페이 안전결제." |

## v13 원본에서 새로 살린 것

`index.html` 의 결제 모달(`openPayModal`)이 **호출되는 곳이 없어 죽어 있었습니다.**
상품 카드마다 **`결제하기`** 버튼을 추가해 모달이 열리도록 연결했습니다.

- 상품 카드 → `결제하기` → 모달(상품명·공급가액·부가세·총액·약관동의) → **네이버페이로 결제하기**
- 상세페이지의 `온라인 결제하기` 는 기존대로 네이버 스마트스토어 직행 (변경 없음)
- `index.html?pay=policy` / `?pay=startup` / `?pay=rnd` / `?pay=venture` 로 접속하면 해당 상품 모달이 자동으로 열립니다

## 배포

이 폴더를 통째로 Netlify **ecoplus24.cloud 사이트**의 Deploys 에 드래그앤드롭.

> ⚠️ 현재 `ecoplus24.cloud` 는 **HTTPS 인증서가 발급되지 않은 상태**입니다.
> Netlify → Domain management → HTTPS → Verify DNS configuration → Provision certificate 로 먼저 해결해 주세요.
